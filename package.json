{
  "name": "server-donasi-netlify-final",
  "version": "1.0.0",
  "description": "Server perantara donasi untuk Roblox",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@netlify/blobs": "^7.0.0"
  }
}
